<% provide(:title, "Home") %>

<div class="row">
  <div class="col-md-3">
    <% if user_signed_in? %>
      <div class="list-selection">
        <%= render partial: "my_lists", locals: { my_lists: @my_lists,
                                             cfiles: @cfiles,
                                             ofiles: @ofiles,
                                             clinks: @clinks,
                                             ctaxons: @ctaxons,
                                             my_failed_lists: @my_failed_lists,
                                             my_subcribing_lists: @my_subcribing_lists }%>

      </div>
    <% end %>
    
    <div class="list-selection">
      <%= render partial: "public_lists", locals: { public_lists: @public_lists } %>
    </div>
  </div>
  
  <div class="col-md-7" id="ra-content">
    <p>
      Welcome to Phylotastic!
    </p>
  </div>
  
  <div class="col-md-2">
    <%= render partial: "creation_nav", locals: { public_lists: @public_lists } %>  
    <div id="nav-list">
    </div>
  </div>  
</div>

<script>
$(document).ready(function() {
  $(".list-group-item").click(function() {
    $(".list-group-item").removeClass("active");
    $(this).addClass("active");
    $('#ra-content').html("<div class='center'><img src='<%= image_path("loading-icon.gif") %>'></div>");
  })
})
</script>