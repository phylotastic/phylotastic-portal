<%= form_for(current_user.watch_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :tree_id, @tree.id %></div>
  <%= f.submit "Add to My Gallery", class: "btn btn-block btn-primary" %>
<% end %>