$('#scale-tree').modal('hide');
$('.modal-backdrop').remove();
$('body').removeClass('modal-open');
get_tree_image("<%= @tree.id %>" , "<%= @tree.sdm_scaled %>", "#img1", 0, 0);
$('#display-scaled-tree').click();
