$('#nav-list').html("<%= escape_javascript(render partial: 'raw_extractions/nav_list_publish', 
                                                  locals: { ra: @ra,
                                                            resolved_names: @resolved_names } ) %>");
$('#ra-content').html("<%= escape_javascript(render partial: 'content', 
                                                    locals: { resolved_names: @resolved_names,
                                                              unresolved:     [],
                                                              name:           @list['list']['list_title'], 
                                                              ra:             @ra} ) %>");