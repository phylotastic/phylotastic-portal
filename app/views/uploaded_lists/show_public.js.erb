$('#ra-holder').html("<%= escape_javascript(render partial: 'content',
                        locals: { resolved_names: @resolved_names,
                                  unresolved:     [],
                                  name:           @list['list']['list_title'], 
                                  ra:             @ra, 
                                  nav_pub_list:   true } ) %>");