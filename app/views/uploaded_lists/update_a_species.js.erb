alert("<%= @mess %>");
$('#ra-content').html("<%= escape_javascript(render partial: 'raw_extractions/content', 
                                                    locals: { resolved_names: @resolved_names,
                                                              unresolved:     @unresolved,
                                                              name:           @list['list']['list_title'], 
                                                              ra:             @ra} ) %>");