if ($("#job-content-Hash-<%= @list['list']['list_id'] %>").length == 0) {
  $("#jobs-content-wrapper").append("<%= escape_javascript(render partial: 'list', locals: { list: @list, uploaded_list: @uploaded_list } ) %>"); 
}
